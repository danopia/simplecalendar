Simple Calendar phpBB3 MOD

This MOD adds a calendar to the site that reads dates from special "event" topics. Forums that contain these events are enabled via the ACP, and only some users can use it.

Main editor: danopia (danopia)
Guest editor: Rick (allingeneral.com)

Instructions on installing the MOD can be found in the MODX file (install_calendar.xml).
